@model IEnumerable<GradingSystem.Models.Subject>
@{
    ViewData["Title"] = "Предмети";
}

<div class="page-header">
    <div>
        <h1>Предмети</h1>
        <p>Всички предмети в системата</p>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> Нов предмет
    </a>
</div>

<div class="table-wrapper">
    <table>
        <thead>
            <tr>
                <th>Предмет</th>
                <th>Съкращение</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="font-weight:600;">@item.Name</td>
                    <td><span class="badge badge-blue">@item.ShortName</span></td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline btn-sm">
                            <i class="fas fa-pen"></i>
                        </a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline btn-sm">
                            <i class="fas fa-eye"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>