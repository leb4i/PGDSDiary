@model GradingSystem.Models.Subject
@{
    ViewData["Title"] = "Редактирай предмет";
}

<div class="page-header">
    <div>
        <h1>Редактирай предмет</h1>
        <p>Промени данните на предмета</p>
    </div>
    <a asp-action="Index" class="btn btn-outline">
        <i class="fas fa-arrow-left"></i> Назад
    </a>
</div>

<div class="card" style="max-width:500px;">
    <div class="card-title">
        <i class="fas fa-book-open" style="color:var(--accent);"></i>
        Данни за предмета
    </div>
    <form asp-action="Edit">
        <div asp-validation-summary="ModelOnly" class="text-danger" style="margin-bottom:12px;"></div>
        <input type="hidden" asp-for="Id" />

        <div class="form-group">
            <label class="form-label">Наименование</label>
            <input asp-for="Name" class="form-control" placeholder="Въведи наименование..." />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label class="form-label">Съкращение</label>
            <input asp-for="ShortName" class="form-control" placeholder="Пример: МАТ, БЕЛ..." />
        </div>

        <div style="display:flex;gap:10px;margin-top:20px;">
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-floppy-disk"></i> Запази
            </button>
            <a asp-action="Index" class="btn btn-outline">Откажи</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}